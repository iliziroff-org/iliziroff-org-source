iliziroff.org — local preview

macOS:
1. Double-click serve.command.
2. Open http://localhost:4173 in Safari/Chrome.
3. Keep the Terminal window open while previewing.

PWA / iPhone:
- PWA features require HTTPS or localhost.
- On iPhone after publishing over HTTPS: Safari → Share → Add to Home Screen.
- The Home Screen icon uses the supplied black/white logo.
